<!-- Centered, width-constrained page shell. Replaces the repeated `mx-auto max-w-* p-6` wrappers;
     the styling lives in the `.ui-page` class (styles/shared/components.css). -->
<template>
    <div class="ui-page">
        <slot />
    </div>
</template>
